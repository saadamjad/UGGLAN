import {Platform, StyleSheet} from 'react-native';
const styles = StyleSheet.create({
// =======TouchableOpacityOfCheckBox=====
TouchableOpacityOfCheckBox:{
    width: 20,
    height: 20,
    backgroundColor: 'transparent',
    borderWidth: 1,
    borderColor: 'white',
    justifyContent: 'center',
    alignItems: 'center',
},
CheckIcon:{
    color: 'white', fontSize: 16
}

});
export default styles;
